version: 2
jobs:
  build:
    docker:
      - image: docker:stable
    steps:
      - checkout
      - run:
          name: wait for dind to be up
          command: sleep 10
      - run:
          name: Build docker image
          command: docker build -t lol .
